import { FilterRamPipe } from './filter-ram.pipe';

describe('FilterRamPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRamPipe();
    expect(pipe).toBeTruthy();
  });
});
