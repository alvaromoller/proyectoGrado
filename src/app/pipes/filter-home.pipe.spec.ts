import { FilterHomePipe } from './filter-home.pipe';

describe('FilterHomePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHomePipe();
    expect(pipe).toBeTruthy();
  });
});
